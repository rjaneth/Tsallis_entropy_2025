\documentclass[11pt]{report}

\usepackage[pdftex]{geometry}
\geometry{a4paper,left=2.5cm,right=2.5cm,top=1.5cm,bottom=2.0cm,twoside}

\usepackage{amssymb}
\usepackage{enumerate}
\usepackage{graphicx}
	\graphicspath{./Comments/}
\usepackage[many]{tcolorbox}
\usepackage{natbib}
\usepackage{hyperref}
\usepackage{subfigure}
\usepackage{bm,bbm}
\usepackage{float}
\usepackage{siunitx}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{xcolor}

\newtcolorbox{reviewbox}[1]{
colback = white!5!white,
colframe = purple!75!black,
fonttitle=\bfseries,
title=#1
}

\newtcolorbox{responsebox}[1]{
	colback = white!5!white,
	colframe = white!75!black,
	fonttitle=\bfseries,
	title=#1
}

\newcommand{\assigned}[1]{\textcolor{blue}{\textbf{#1}}}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{document}


\begin{center}
\large{\textbf{Response Letter to Reviewer \# 1}}

\vglue 0.3cm

\huge{ Adaptive Model-Free Tsallis Entropy Estimation for Detecting Non-Fully-Developed Speckle\\ (TGRS-2025-10064)}
\end{center}

%\authors
\begin{center}
\textbf{Janeth Alpala,  Alejandro C.\ Frery, Paolo Gamba, Abraão D. C. Nascimento, Andrea A. Rey }
\end{center}

\date{\today}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\vspace{0.5cm}
\noindent Dear Editor
\bigskip

\noindent We are grateful to the reviewers for the valuable comments and the time spent on checking our manuscript. 
We have addressed their comments towards improving the paper contents and presentation. 
Below we detail the changes and updates incorporated into the manuscript in this round of review.

\medskip

%-------------------------------------
\begin{reviewbox}{Comment 1 }
The core of the proposed method relies on Tsallis entropy, which introduces an additional entropic index $q$. However, the manuscript fails to provide a clear physical justification for the choice of $q$ in the context of SAR backscattering. The authors should explain how specific values of $q$ relate to different degrees of non-additivity in radar returns, or how they correspond to specific physical clutter models (e.g., $K$-distribution or $\mathcal{G}^{0}$ distribution).
\end{reviewbox}

\begin{responsebox}{Response}

For simplicity, our reply will focus on discrete probabilities of the form $(p_i)_{I\geq 1}$.

\citet{Tsallis1988} observed that in multifractal analysis, it is customary to scale probabilities raising them to a positive power $\lambda$, termed ``entropic index,'' because using $p_i^\lambda$ renders ``richer and more complex [behavior]'' \citep{FractalMeasuresandTheirSingularitiestheCharacterizationofStrangeSets}.
Tsallis took that idea and defined the quantity 
$$
S_\lambda = k \frac{1-\sum_{i} p_i^\lambda}{\lambda-1}
$$
``to generalize the standard expression of the entropy $S$ in information theory''.
He then proved that $S_\lambda$ has several useful properties that collapse to classical results when $\lambda\to1$.
This variant of entropy later received his name and has been extensively used.
In our article, we denote the Tsallis entropy as $T_\lambda$.

The article by \citet{ImageThresholdingUsingTsallisEntropy} is among the first to use the Tsallis entropy for image analysis, specifically for optical image segmentation.
The authors state that
\begin{quote}
The parameter $\lambda$ in Tsallis entropy is usually
interpreted as a quantity characterizing the degree
of nonextensivity of a system. An appropriate
choice of the entropic index $\lambda$ to nonextensive
physical systems still remains an open field of
study. In some cases the parameter $\lambda$ has no
physical meaning, but it gives new possibilities in
the agreement of theoretical models and experi-
mental data [\dots]. In
othercases, $\lambda$ is solely determined by constraints of
the problem and thereby $\lambda$ may have a physical
meaning [\dots].
\end{quote}
\end{responsebox}

\begin{responsebox}{}
In line with this work, we used grid search to find the optimal value of $\lambda$ for our problem and found that $\lambda=0.85$ yielded the smallest bias and mean squared error in all but one case.
Interestingly, \citet{TheApplicationofTsallisEntropyBasedSelfAdaptiveAlgorithmforMultiThresholdImageSegmentation} found that $\lambda=0.80$ was the best parameter for optical image segmentation.

There is an interpretation for the optimal value we found ($\lambda=0.85$).
The summands that define $S_\lambda$ are power transformations of probabilities.
If $\lambda=1$, $S_\lambda$ reduces to the Shannon entropy.
If $\lambda>1$, then ``large'' probabilities have more weight in the sum.
If $\lambda<1$, then the effect of ``small'' probabilities is amplified.
The difference between the gamma and $\mathcal G^0$ models lies mostly in the tails, i.e., in how these laws describe rare events.
Then, $\lambda<1$ highlights this difference, serving the purpose of the test we proposed.

In the manuscript, we have added the following clarification in Section~III.A:
\begin{quote}
\textcolor{blue}{There is an interpretation for this optimal value of $\lambda$.
When considering discrete probabilities, smaller values this entropic index
amplify the effect of ``small'' probabilities on the total Tsallis entropy.
The $\mathcal G^0$ and gamma distributions differentiate mostly on their tails,
so enhancing the effect of the tails improves our ability to tell one from the other.}
\end{quote}
\end{responsebox}

\vspace{1em}

%-------------------------------------
\begin{reviewbox}{Comment 2}
While the bootstrap correction is intended to improve accuracy for small sample sizes, the paper does not discuss the computational cost associated with performing bootstrap resampling for every pixel or local window. Furthermore, in highly heterogeneous regions, the bootstrap might inadvertently sample across structural boundaries, leading to biased estimates. A more thorough analysis of the bootstrap’s variance reduction versus its potential bias in edge regions is needed.
\end{reviewbox}

\begin{responsebox}{Response}
Regarding the additional computational time that bootstrap imposes on the technique, we have added the following information:
\begin{quote}
The total runtime also depends on the number of bootstrap replications, since each replication recomputes the test statistic to obtain a more stable estimate of the Tsallis entropy measure. In our simulations, we used $B=100$, which provides stable and reliable estimates with moderate computational cost \textcolor{blue}{(the running time increases by, at most, a factor of $15$)}. 
\end{quote}

Heterogeneous regions produce outliers
with respect to the iid model 
that may have a detrimental effect in resampling techniques.
\citet{JackknifeAfterBootstrapStandardErrorsandInfluenceFunctions} suggested checking bootstrap samples with a jackknife procedure to identify outliers.
After that seminal work, many proposals have been studied, e.g., using the influence function \citep{RobustBootstrapwithNonRandomWeightsBasedontheInfluenceFunction}.
To the best of our knowledge, those approaches consider a fixed sample size.
Our proposal follows a different path: the adaptive windows are expected to discard samples which are in disagreement with the observations around the center pixel.
Such trimming may reach almost \SI{80}{\percent} of the observations when reducing from a window of size $11\times11$ pixels to another of size $5\times5$ pixels.

The new version includes this clarification at the end of Section~III.B:
\begin{quote}
\textcolor{blue}{Heterogeneous samples occur when the window spans two or more different areas, 
e.g., across an edge.
They produce outliers that may have a detrimental effect in resampling techniques.
In Sec.~III.F we use adaptive windows to discard samples which are in disagreement with the observations around the center pixel.
Such trimming may reach almost \SI{80}{\percent} of the observations when reducing from a window of size $11\times11$ pixels to another of size $5\times5$ pixels.
}
\end{quote}

\end{responsebox}

\vspace{1em}

%-------------------------------------
\begin{reviewbox}{Comment 3}
The manuscript claims that the proposed tool does not require ``scene-specific calibration.'' However, nonparametric entropy estimators are known to be sensitive to the quantization of the input data (bit depth) and to any prior despeckling filters applied. The authors should include a sensitivity analysis showing how the $p$-value maps change when the input SAR data are scaled or filtered differently.
\end{reviewbox}

\begin{responsebox}{Response}
The effect of the quantisation has been studied, among others, by \citet{beirlant1997nonparametric} and, as noted by the reviewer, it is not negligible when the number of possible values (bins) is not too big.
In our case, the SAR images we used are stored in single or double precision.
The IEEE 754 Standards stipulates that these formats are able to encode $2^{32}$ and $2^{64}$ bit patterns.
Such amount of possible values has no effect on the estimation.

Moreover, we do not apply any despeckling filter.

\end{responsebox}

\vspace{1em}


%-------------------------------------
\begin{reviewbox}{Comment 4}
In the Related Work section, the authors focus heavily on classical information theory and basic statistical SAR modeling. While these are foundational, the discussion lacks a connection to contemporary SAR target detection and anomaly detection methodologies (2024--2025), such as ORPDet, PDE-guided approaches, BurgsVO, and S4Det.
\end{reviewbox}

\begin{responsebox}{Response}

We have added the following paragraphs to the introduction.

\begin{quote}
\textcolor{blue}{
Deep learning has driven significant advances in SAR image analysis.
In particular, for ship detection, architectures ranging from lightweight fully convolutional networks \citep{ORPSDOuterRectangularProjectionBasedRepresentationforOrientedShipDetectioninSARImages} to partial differential equations-guided and transformer-based models \citep{PDEGuidedDiverseFeatureLearningforSARRotatedShipDetection, BurgsVOBurgsAssociatedVertexOffsetEncodingSchemeforDetectingRotatedShipsinSARImages, S4DetBreadthandAccurateSineSingleStageShipDetectionforRemoteSenseSARImagery}
lead to successful applications.
Despite their strong performance, these methods share three fundamental limitations: (i) heavy dependence on large labeled datasets; (ii) limited physical interpretability; and (iii) sensitivity to domain shift.}

\textcolor{blue}{
In this work, we propose a method that exploits the statistical properties of SAR imagery through Tsallis entropy whose non-extensive parameter $\lambda$ exploits the heavitailednes of the models for SAR imagery. 
The resulting approach requires no retraining or hyperparameter tuning, does not depend on sensors and resolutions, and yields fully interpretable results. 
The method offers an alternative when labeled data are scarce, interpretability is required, or computational resources are constrained.
It can serve as a feature layer within explainable machine learning pipelines.
}
\end{quote}
\end{responsebox}

%-------------------------------------

\bibliography{../../../references}
\bibliographystyle{agsm}

\end{document}

